const attr = {
	callback: 'data-toggle-callback',
	pressed: 'aria-pressed',
};

const classes = {
	active: 'mdf-button--active',
};

const events = {
	changed: 'MDFToggle:changed',
};

const selectors = {
	toggle: '.mdf-button--toggle',
};

export { attr, classes, events, selectors };
