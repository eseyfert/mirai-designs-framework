const classes = {
	fadeIn: 'mdf-grid__item--fade-in',
};

const selectors = {
	grid: '.mdf-grid',
	item: '.mdf-grid__item',
	itemContent: '.mdf-grid__item-content',
};

export { classes, selectors };
